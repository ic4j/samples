- route:
    id: "ic"
    from:
      uri: "kafka:icTopic?brokers=localhost:9092"
      steps:
        - setHeader:
                name: "kafka.KEY"
                expression:
                  constant:
                    expression: "Camel"
        - to: "kafka:icTopic?brokers=localhost:9092"
